---
version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Install tget
          command: npm install -g torrent-dl
      - run:
          name: Download torrent
          command: torrent-dl -a your-torrent-file.torrent --output ./downloads/
workflows:
  version: 2
  build:
    jobs:
      - build
